# Personal design and profile documents — local only, never publish
CLAUDE.md
design_spec.md
news_profile.md
roadmap.md
docs/

# Local session notes (prompt templates, scratch)
archive/_*.txt

# Runtime state JSON — 6 categories committed for GHA dedup persistence
# (concept_history / cooking_history / displayed_urls_*.json /
# market_history / page4_rotation / page5_history). Audit logs (scores,
# llm_usage, page2_scores, page3_selection) and the local cron wrapper
# log are ignored; audit logs are kept for 90 days as GHA Artifacts.
logs/scores_*.json
logs/llm_usage_*.json
logs/cron_*.log
logs/*.bak
logs/page2_scores_*.json
logs/page3_selection_*.json

# Local experimentation — scripts and outputs that should not deploy
experiment/

# Backup files from local edits
*.backup

# Local Claude Code settings — track shared settings.json, ignore the rest
.claude/*
!.claude/settings.json

# Python artifacts
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.vscode/
.idea/
