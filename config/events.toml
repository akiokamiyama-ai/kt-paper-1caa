# Header カウントダウン用の次回イベント。
# 神山さんが手動更新する。過ぎたイベントは header から自動的に消える。
#
# 将来複数イベント対応する場合は [[events]] 配列形式に拡張可能（Phase 2）。
#
# 表示形式は scripts/header/countdown.py::format_countdown が
#   f"{name}まで {days}日"
# を組む。``name`` はそのまま前置されるので、**表示に出したい短い呼称**を書く
# こと（正式名称を入れると「フジロック・フェスティバル 2027まで 342日」の
# ように読みにくくなる）。正式名称は下のコメントに残す。
#
# C165 (Sprint 13, 2026-08-15): フジロック 2026-07-24（開催済）から 2027 開催日に更新。
# 旧設定は過去日だったため compute_days_until が None を返し、
# masthead-data-row1 からカウントダウン欄が消えていた。

[next_event]
# 正式名称: フジロック・フェスティバル 2027（苗場スキー場、2027-07-23 開幕）
name = "フジロック"
date = "2027-07-23"
